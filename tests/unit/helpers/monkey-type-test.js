import { monkeyType } from 'monkeylist/helpers/monkey-type';
import { module, test } from 'qunit';

module('Unit | Helper | monkey type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = monkeyType([42]);
  assert.ok(result);
});
